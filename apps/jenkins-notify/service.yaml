apiVersion: v1
kind: Service
metadata:
  name: jenkins-notify-service
  namespace: devops-tools
  labels:
    app: jenkins-notify
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: jenkins-notify
