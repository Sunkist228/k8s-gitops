apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devops-tools

resources:
- rbac.yaml
- config.yaml
- deployment.yaml
- service.yaml
- ingress.yaml

commonLabels:
  app: ingress-manager-api
