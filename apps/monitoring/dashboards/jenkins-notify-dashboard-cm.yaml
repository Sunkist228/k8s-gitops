apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-jenkins-notify
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  jenkins-notify.json: |
    {
      "title": "Jenkins Notify Service",
      "timezone": "browser",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "30s",
      "tags": ["jenkins-notify", "devops-tools"],
      "templating": {
        "list": [
          {
            "name": "namespace",
            "type": "custom",
            "query": "devops-tools",
            "current": {
              "selected": true,
              "text": "devops-tools",
              "value": "devops-tools"
            },
            "hide": 0
          },
          {
            "name": "service",
            "type": "custom",
            "query": "jenkins-notify-service",
            "current": {
              "selected": true,
              "text": "jenkins-notify-service",
              "value": "jenkins-notify-service"
            },
            "hide": 0
          },
          {
            "name": "pod",
            "type": "query",
            "query": "label_values(kube_pod_info{namespace=\"$namespace\",pod=~\"jenkins-notify.*\"}, pod)",
            "includeAll": true,
            "multi": true,
            "allValue": ".*",
            "current": {
              "selected": true,
              "text": "All",
              "value": "$__all"
            },
            "refresh": 1
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "Target Up",
          "type": "stat",
          "gridPos": {"h": 4, "w": 6, "x": 0, "y": 0},
          "targets": [
            {
              "expr": "sum(up{namespace=\"$namespace\",service=\"$service\"}) or vector(0)",
              "refId": "A"
            }
          ]
        },
        {
          "id": 2,
          "title": "Pod Restarts",
          "type": "stat",
          "gridPos": {"h": 4, "w": 6, "x": 6, "y": 0},
          "targets": [
            {
              "expr": "sum(kube_pod_container_status_restarts_total{namespace=\"$namespace\",pod=~\"$pod\"}) or vector(0)",
              "refId": "A"
            }
          ]
        },
        {
          "id": 3,
          "title": "HTTP RPS",
          "type": "stat",
          "gridPos": {"h": 4, "w": 6, "x": 12, "y": 0},
          "targets": [
            {
              "expr": "sum(rate(http_requests_total{namespace=\"$namespace\",service=\"$service\"}[5m])) or vector(0)",
              "refId": "A"
            }
          ]
        },
        {
          "id": 4,
          "title": "5xx Ratio",
          "type": "stat",
          "gridPos": {"h": 4, "w": 6, "x": 18, "y": 0},
          "targets": [
            {
              "expr": "(sum(rate(http_requests_total{namespace=\"$namespace\",service=\"$service\",status=~\"5..\"}[5m])) / clamp_min(sum(rate(http_requests_total{namespace=\"$namespace\",service=\"$service\"}[5m])), 0.1)) or vector(0)",
              "refId": "A"
            }
          ]
        },
        {
          "id": 5,
          "title": "CPU Usage by Pod (cores)",
          "type": "timeseries",
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 4},
          "targets": [
            {
              "expr": "sum by (pod) (rate(container_cpu_usage_seconds_total{namespace=\"$namespace\",pod=~\"$pod\",container!=\"\",image!=\"\"}[5m]))",
              "legendFormat": "{{pod}}",
              "refId": "A"
            }
          ]
        },
        {
          "id": 6,
          "title": "Memory Usage by Pod (MiB)",
          "type": "timeseries",
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 4},
          "targets": [
            {
              "expr": "sum by (pod) (container_memory_working_set_bytes{namespace=\"$namespace\",pod=~\"$pod\",container!=\"\",image!=\"\"}) / 1024 / 1024",
              "legendFormat": "{{pod}}",
              "refId": "A"
            }
          ]
        },
        {
          "id": 7,
          "title": "HTTP Request Rate by Status",
          "type": "timeseries",
          "gridPos": {"h": 8, "w": 12, "x": 0, "y": 12},
          "targets": [
            {
              "expr": "sum by (status) (rate(http_requests_total{namespace=\"$namespace\",service=\"$service\"}[5m])) or vector(0)",
              "legendFormat": "{{status}}",
              "refId": "A"
            }
          ]
        },
        {
          "id": 8,
          "title": "p95 Latency (seconds)",
          "type": "timeseries",
          "gridPos": {"h": 8, "w": 12, "x": 12, "y": 12},
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\",service=\"$service\"}[5m]))) or vector(0)",
              "refId": "A"
            }
          ]
        }
      ],
      "annotations": {"list": []}
    }
