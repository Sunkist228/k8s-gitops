# Kustomize Shared Components

This directory is intended for shared Kustomize bases and configurations that are used across multiple applications.
Currently, each application in `apps/` manages its own Kustomize configuration.
